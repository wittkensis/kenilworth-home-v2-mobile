# Password the user types at login screen
APP_PASSWORD=your-password-here

# Random token stored in the auth cookie — generate with: openssl rand -hex 32
AUTH_TOKEN=your-random-token-here

# SQLite database path (defaults to ./data/home.db in dev, /data/home.db in prod)
DB_PATH=./data/home.db
